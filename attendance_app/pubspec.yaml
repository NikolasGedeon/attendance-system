name: attendance_app
description: Attendance App frontend for the NestJS attendance API.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_secure_storage: ^9.2.2
  geolocator: ^13.0.1
  file_picker: 10.3.3
  local_auth: ^2.3.0
  mobile_scanner: ^7.2.0
  qr_flutter: ^4.1.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
